[project]
name = "incentive-drive-api"
version = "0.1.0"
description = "IncentiveDrive API - Automotive incentive-based lead generation"
requires-python = ">=3.12"

[tool.ruff]
target-version = "py312"
line-length = 120

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"
